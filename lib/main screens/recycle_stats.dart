import 'package:flutter/material.dart';

class RecycleStats extends StatelessWidget {
  RecycleStats({super.key});
  Widget build(BuildContext context) {
    return Container(
      color: Colors.white,
    );
  }
}

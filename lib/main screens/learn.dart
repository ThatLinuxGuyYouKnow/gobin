import 'package:flutter/material.dart';

class LearnMore extends StatelessWidget {
  List<String> wasteTypes = ['plastic', 'glass', 'metal', 'paper'];
  Widget build(BuildContext context) {
    return Container(
      color: Colors.white,
      child: Column(
        children: [],
      ),
    );
  }
}
